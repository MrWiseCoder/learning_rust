mod loops;
mod homework;

fn main() {
    // println!("Hello, world!");
    let mut val: i32;
    // ***************************************
    // loops::main2();
    // loops::main3();
    // loops::main4();
    // loops::main5();
    // loops::main6();
    // ***************************************
    // let celc = homework::convert_from_fahr_to_celc(200);
    // println!("for Fahrenheit 200 Celcius = {:.2}", celc);
    // let celc = homework::convert_from_fahr_to_celc(40);
    // println!("for Fahrenheit 40 Celcius = {:.2}", celc);
    // let celc = homework::convert_from_fahr_to_celc(100);
    // println!("for Fahrenheit 100 Celcius = {:.2}", celc);
    // ***************************************
    // val = 90;
    // let fahr = homework::convert_from_celc_to_fahr(val);
    // println!("for Celcius {:.2} Fahrenheit = {:.2}", val, fahr);
    // val = 4;
    // let fahr = homework::convert_from_celc_to_fahr(val);
    // println!("for Celcius {:.2} Fahrenheit = {:.2}", val, fahr);
    // val = 60;
    // let fahr = homework::convert_from_celc_to_fahr(val);
    // println!("for Celcius {:.2} Fahrenheit = {:.2}", val, fahr);
    // ***************************************
    // println!("fib 0 = {}", homework::fibn(0));
    // println!("fib 1 = {}", homework::fibn(1));
    // println!("fib 2 = {}", homework::fibn(2));
    // println!("fib 3 = {}", homework::fibn(3));
    // println!("fib 10 = {}", homework::fibn(10));
    // println!("fib 50 = {}", homework::fibn(50));
    // ***************************************
    homework::lyrics();
}
